<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lhl.keshang.customer.mapper.DiodeMapper">

    <select id="diodeList" resultType="diode">

        SELECT ID,NAME,PID FROM BD_DIODE

    </select>

</mapper>